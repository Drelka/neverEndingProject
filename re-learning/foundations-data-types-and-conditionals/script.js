let siblings = null; // null is it's own type, null, not a number nor a string
// in js represents "nothing", "empty", "value unknown"

let education = undefined; // also type of it's own, like null
// the meaning of undefined is "value is not assigned"

